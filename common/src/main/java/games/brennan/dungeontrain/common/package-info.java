/**
 * Loader-neutral Dungeon Train code shared by the NeoForge (root) and Fabric modules.
 *
 * <p>Empty in the Phase 1 multiloader skeleton — loader-neutral classes migrate here
 * in Phase 2. Compiled in ModDevGradle "vanilla mode" (NeoForm, no mod loader) so it
 * stays free of NeoForge- and Fabric-specific API references.</p>
 */
package games.brennan.dungeontrain.common;
